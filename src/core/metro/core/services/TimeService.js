const logger = require('../../../../events/logger');

class TimeService {
    constructor(metro) {
        this.metro = metro;
    }

    checkTime() {
        // This method is no longer in use.
    }
}

module.exports = TimeService;
